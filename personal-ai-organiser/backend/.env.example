# Google Cloud Credentials
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback # Must match Google Cloud Console

# Notion API Credentials
EMAIL_SENDER_ADDRESS=your-email@gmail.com
EMAIL_APP_PASSWORD=YOUR_GMAIL_APP_PASSWORD # If using Gmail SMTP + 2FA
EMAIL_RECIPIENT_ADDRESS=recipient-email@example.com # Where to send the plan
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465 # For SSL

# Session Management
SESSION_SECRET_KEY=YOUR_VERY_STRONG_RANDOM_SECRET_KEY # Generate a strong random key

# Frontend URL (for redirects)
FRONTEND_URL=http://localhost:5173 # Adjust if your frontend runs elsewhere

# Add other relevant email settings (e.g., App Password if using SMTP) 